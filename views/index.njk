{# <!DOCTYPE html>
<html lang="en" class="bg-gray-100 text-gray-900">
  <head>
    <meta charset="UTF-8" />
    <title>AI Chatbot</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="flex items-center justify-center min-h-screen p-4">
    <div class="w-full max-w-xl bg-white rounded-2xl shadow-xl p-6 space-y-4">
      <h1 class="text-2xl font-bold text-center text-indigo-600">Saarth AI Chatbot</h1>

      <form method="POST" action="/chat" class="space-y-4">
        <input
          type="text"
          name="message"
          placeholder="Ask something (e.g., define AI)"
          required
          class="w-full px-4 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring focus:border-indigo-300"
        />
        <button
          type="submit"
          class="w-full bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700 transition"
        >
          Ask AI
        </button>
      </form>

      {% if response %}
      <div class="mt-4 p-4 border-t">
        <h2 class="font-semibold text-gray-700 mb-2">Response:</h2>
        <pre class="whitespace-pre-wrap text-sm">{{ response | safe }}</pre>
      </div>
      {% endif %}
    </div>
  </body>
</html> #}


<!DOCTYPE html>
<html lang="en" class="bg-gradient-to-br from-slate-100 to-white text-gray-900">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Claude Chatbot</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>
  <body class="min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-2xl bg-white/70 backdrop-blur-sm rounded-2xl shadow-xl p-6 space-y-6 border border-gray-200">
      <h1 class="text-3xl font-semibold text-indigo-600 text-center">💬 Saarthi </h1>

      <form method="POST" action="/chat" class="flex flex-col md:flex-row gap-3">
        <input
          type="text"
          name="message"
          placeholder="Ask something (e.g. define AI, how to deploy...)"
          required
          class="flex-1 px-4 py-3 rounded-xl border shadow-sm focus:ring-2 focus:ring-indigo-400 focus:outline-none text-base"
        />
        <button
          type="submit"
          class="bg-indigo-600 text-white px-6 py-3 rounded-xl hover:bg-indigo-700 transition"
        >
          Ask
        </button>
      </form>

      {% if response %}
      <div class="space-y-4">
        <div class="flex items-start gap-3">
          <div class="w-10 h-10 bg-indigo-600 text-white flex items-center justify-center rounded-full font-bold">AI</div>
          <div class="bg-white border border-gray-200 p-4 rounded-xl shadow-sm w-full text-sm leading-relaxed whitespace-pre-wrap">
            {{ response | safe }}
          </div>
        </div>
      </div>
      {% endif %}

      <p class="text-center text-xs text-gray-500 mt-6">
        Saarthi AI Chatbot • Made with ❤️ by Saurabh Yadav
      </p>
    </div>
  </body>
</html>
